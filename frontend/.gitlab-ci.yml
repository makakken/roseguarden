default:
  image: node:latest
  before_script:
    - cd frontend
    - npm ci --cache .npm --prefer-offline

frontend:
  stage: build 
  script:
    - npm run build
  artifacts:
    paths:
      - frontend/static
  only:
    - master
